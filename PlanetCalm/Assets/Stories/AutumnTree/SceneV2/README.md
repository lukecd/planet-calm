# Autumn Tree layout revision V2

`terrain-adjustments.json` layers a terrain-overlap correction over
`SceneV1/compositions.json`. It moves the visible top of `ground-base`
upward while extending its rendered height by the same amount, closing the
foreground seam without modifying source artwork or other placement data.
